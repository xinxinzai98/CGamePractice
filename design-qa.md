# 视觉重构验收

final result: passed

验收范围为《当前视觉方案.md》定义的风格、信息分区和真实玩法映射；不是生成图的逐像素复刻，也不表示新插画素材整套制作完成。

- 参考：`../outputs/游戏界面参考-20260911/当前视觉方案.md`、`04-hangar.png`、`v2/mecha.png`、`v2/member.png`、`v2/battle.png`。
- 实现：http://127.0.0.1:5173 ，Codex in-app Browser，1280×720 CSS viewport，DPR 1，1600×900 舞台等比缩放 0.8。
- 原参考 1672×941（机库）及各 V2 原图；比较图统一缩放为 1280×720，实际截图为 1280×720。原图未修改。
- 截图：`work/visual-qa/hangar.png`、`research.png`、`members.png`、`magi.png`、`battle.png`、`training.png`。
- 同屏对比：`work/visual-qa/*-comparison.jpg`。参考与实际账号/关卡并不相同，数值、节点和关卡结构按当前真实内容核验，不根据示意图硬填。

## 检查结果

字体：中文标题使用 Songti SC / STSong 宋体，正文系统黑体、英文等宽。标题与导航层级明确。人物长名允许换行。

布局：导航 220px，内容区与战场均在固定舞台内。机库全部操作和 MAGI A/B 开始按钮在首屏可见。研究/成员内容滚动，人物、拥有对象和节点详情固定于内容顶部。访客说明正常占位，无覆盖主操作。

颜色：深炭黑、象牙白、橙红主操作与青绿状态；1P 橙红、2P 青绿。禁用研究操作保留原因与对比度。

素材：复用项目已有机库、机体、人物、贴图与技能资产。机库前景取消 screen 混合，避免机体透出背景。战场是实时 Phaser 地图而非截图。保留原有精灵与碰撞地图，因此精灵细节、地图密度和特效不会与概念图相同。

内容：研究和成员是独立入口；成员使用真实培养数据、能力数和熟练度，未加入示意等级。节点选中后右侧详情准确更新。未添加战中切机、行动点或回合制。

细节检查已在原始 1280×720 页面观察完成：导航/按钮、人物裁切、节点选中边框与详情、底栏和双人状态卡；同屏对比用来复核整体分区和视觉风格。

## 修正历史

1. P2：机库及 MAGI 首屏按钮被访客提示遮挡。改 flex 高度与独立滚动、缩短卡片，最终 hangar/magi 截图已无覆盖。
2. P2：机库编号显示 `se`。改显式 00/01/02/08 映射。
3. P2：训练仍为鲜绿草地。复用石质纹理并调整 tint，保留地形值；training 截图已更新。
4. P2：两人 HUD 第二张卡与底部提示重叠。压缩卡片和品牌区，battle 截图已无重叠。
5. P2：研究人物被长节点列表推到首屏以下。改固定高度 sticky 人物栏；research/members 最终截图已可见人物与说明。
6. P2：机库 screen 混合导致透明感。改普通合成及顶部对齐，hangar 最终图头部完整。

## 验证与边界

构建及 TypeScript 检查通过。完整测试 248 通过、0 失败（`work/visual-tests.log`）。最终 CSS 修改后再次 build 通过（`work/visual-build.log`）。构建存在约 1.8 MB 主 JS 包体积提示，不影响编译。

浏览器实测：封面→登录→访客训练，训练暂停/恢复/离开，所有主导航，机甲节点与详情，成员页面，后勤、补给及档案空状态，设置弹窗，MAGI A 配置启动→双机战场→射击→暂停→返回及中止记录。最终浏览器 error 日志为空。

未重新执行真实账号双浏览器正式对局及付费/物资写入；本轮未改服务端和成长规则，已有网络/存储测试纳入完整回归。概念图里的插画级战斗资产仍以现有资源代替，属于明确保留的素材差异。
